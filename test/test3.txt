import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  standalone: true,
  template: `
    <div class="container">
      <h1>Count: {{ count }}</h1>
      <button (click)="increment()">Increment</button>
    </div>
  `,
  styles: [`
    .container {
      text-align: center;
      margin-top: 20%;
      font-family: Arial, sans-serif;
    }

    button {
      padding: 10px 20px;
      font-size: 16px;
      background-color: #007bff;
      color: white;
      border: none;
      border-radius: 6px;
      cursor: pointer;
    }

    button:hover {
      background-color: #0056b3;
    }
  `]
})
export class AppComponent {
  count = 0;

  increment() : int {
    this.count = this.count + 1;
    let maxCount : int = 10 * 5 / 3;
  }

   increment2() : int {
     let maxCount2 : int = 10 * 5 / 3;
     increment(name);
   }
}
